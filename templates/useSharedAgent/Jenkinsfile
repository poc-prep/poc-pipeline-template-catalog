pipeline {
  agent none
  stages {
    stage('Build') {
      agent {
        node {
          label 'sharedAgent'
        }

      }
      steps {
        echo 'Build Stage'
      }
    }
    stage('Dev') {
      agent {
        node {
          label 'sharedAgent'
        }

      }
      steps {
        echo 'Dev Stage'
      }
    }
    stage('Test') {
      agent {
        node {
          label 'sharedAgent'
        }

      }
      steps {
        echo 'Test Stage'
      }
    }
    stage('Prod') {
      agent {
        node {
          label 'sharedAgent'
        }

      }
      steps {
        echo 'Prod Stage'
      }
    }
  }
}
